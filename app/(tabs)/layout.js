import NavTabs from "../../components/NavTabs";

export const metadata = {
  title: "Holy Padel — Clubhuis",
  description: "Dashboard, events, winkel, foto’s, socials en reserveringen.",
};

export default function TabsLayout({ children }){
  return (
    <div>
      {children}
    </div>
  );
}
